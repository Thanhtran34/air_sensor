{
  "name": "assignment-iot",
  "version": "1.0.0",
  "description": "A web app to evaluate air quality",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "start:dev": "nodemon --inspect -r dotenv/config src/server.js",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.lnu.se:1dv027/student/tt222kj/assignment-iot.git"
  },
  "author": "Thanh Tran",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.0.1",
    "eslint": "^8.16.0",
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "nodemonConfig": {
    "ext": "js,json,hbs,html,css"
  }
}
