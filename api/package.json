{
  "name": "api",
  "version": "1.0.0",
  "description": "A web app to evaluate air quality",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:dev": "nodemon --inspect -r dotenv/config server.js",
    "lint": "npx eslint ./api || exit 0",
    "lint:fix": "npx eslint ./api --fix || exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thanh Tran",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.0.1",
    "eslint": "^8.16.0",
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "firebase-admin": "^10.2.0",
    "path": "^0.12.7",
    "url": "^0.11.0"
  }, 
  "nodemonConfig": {
    "ext": "js,json,hbs,html,css"
  }

}
